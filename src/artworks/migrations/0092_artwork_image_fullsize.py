# Generated by Django 4.2.15 on 2024-10-25 13:00
import logging
from django.db import migrations
import versatileimagefield.fields
import artworks.models

logger = logging.getLogger(__name__)

def create_fullsize_images(apps, schema_editor):
    from artworks.models import Artwork
    for artwork in Artwork.objects.all():
        if hasattr(artwork, 'create_image_fullsize') and callable(artwork.create_image_fullsize) and artwork.image_original:
            artwork.create_image_fullsize()
        else:
            logger.warning('Artwork model does not have a create_image_fullsize method')
            break

class Migration(migrations.Migration):

    dependencies = [
        ('artworks', '0091_remove_artwork_material_old_and_more'),
    ]

    operations = [
        migrations.AddField(
            model_name='artwork',
            name='image_fullsize',
            field=versatileimagefield.fields.VersatileImageField(blank=True, max_length=255, null=False, upload_to=artworks.models.get_path_to_image_fullsize, verbose_name='Fullsize Image'),
        ),
        migrations.RunPython(
            create_fullsize_images,
            reverse_code=migrations.RunPython.noop,
        ),
    ]
