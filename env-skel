## These settings are used to set up the DB in the image-postgres container.
DB_NAME=django_image
DB_USER=django_image
## Make sure to change this to a strong password on any production/public server.
## If you change it here for local development, make sure to also set the POSTGRES_PASSWORD
## in src/image/.env accordingly
DB_PASSWORD=password

## The directory where your (uploaded) image files should reside
MEDIA_DIR=./src/assets/media
